local request = request or syn.request or http_request
if request then
    print("we got request")
    local res = request({ Url = "https://google.com", Method = "GET" })
    print(res)
else
    print("we dont got request")
end
print(request)